import { describe, it } from 'vitest';

describe('i18n locale routing', () => {
  it.todo('routing config defines pt and en locales');
  it.todo('routing config sets pt as default locale');
  it.todo('messages/pt.json contains all required translation keys');
  it.todo('messages/en.json contains all required translation keys');
  it.todo('pt.json and en.json have identical key structures');
});
