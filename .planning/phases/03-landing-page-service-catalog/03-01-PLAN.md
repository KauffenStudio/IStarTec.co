---
phase: 03-landing-page-service-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jetwash24/src/lib/catalog.ts
  - jetwash24/src/__tests__/catalog.test.ts
  - jetwash24/messages/pt.json
  - jetwash24/messages/en.json
  - jetwash24/src/app/globals.css
  - jetwash24/src/components/ui/tabs.tsx
  - jetwash24/src/components/ui/card.tsx
  - jetwash24/src/components/ui/badge.tsx
  - jetwash24/src/components/ui/separator.tsx
autonomous: true
requirements:
  - SERV-02
  - SERV-04

must_haves:
  truths:
    - "Price calculation returns base_price + surcharge for any vehicle type"
    - "Package savings calculation returns the difference between individual component total and bundle price"
    - "All translation keys for hero, about, services, contact, and nav exist in both PT and EN"
    - "Smooth scroll behavior is enabled on the html element"
  artifacts:
    - path: "jetwash24/src/lib/catalog.ts"
      provides: "Pure functions for price calculation, savings calculation, slug-to-category mapping, package composition mapping"
      exports: ["calculatePrice", "calculateSavings", "SLUG_CATEGORY", "PACKAGE_COMPONENTS"]
    - path: "jetwash24/src/__tests__/catalog.test.ts"
      provides: "Unit tests for calculatePrice and calculateSavings"
      min_lines: 30
    - path: "jetwash24/messages/pt.json"
      provides: "All Portuguese translation keys for Phase 3"
      contains: "HomePage.tagline"
    - path: "jetwash24/messages/en.json"
      provides: "All English translation keys for Phase 3"
      contains: "HomePage.tagline"
  key_links:
    - from: "jetwash24/src/__tests__/catalog.test.ts"
      to: "jetwash24/src/lib/catalog.ts"
      via: "import { calculatePrice, calculateSavings }"
      pattern: "import.*calculatePrice.*from.*catalog"
---

<objective>
Install shadcn UI components, create testable pure functions for price/savings calculations, add all Phase 3 translation keys, and enable smooth scroll.

Purpose: Establishes the foundation that all subsequent Phase 3 plans depend on -- shadcn components for the UI, pure logic functions that are unit-tested before any component uses them, and complete i18n keys so components can reference translations immediately.
Output: 4 shadcn components installed, catalog.ts with tested pure functions, complete PT+EN translations, smooth scroll CSS.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-landing-page-service-catalog/03-CONTEXT.md
@.planning/phases/03-landing-page-service-catalog/03-RESEARCH.md
@.planning/phases/03-landing-page-service-catalog/03-UI-SPEC.md
@.planning/phases/03-landing-page-service-catalog/03-VALIDATION.md

<interfaces>
<!-- Existing types the executor must use -->

From jetwash24/src/types/database.ts:
```typescript
export type VehicleType = 'citadino' | 'berlina' | 'suv' | 'carrinha';

export interface Service {
  id: string;
  slug: string;
  name_pt: string;
  name_en: string;
  desc_pt: string | null;
  desc_en: string | null;
  duration_min: number;
  base_price: number;
  is_active: boolean;
  sort_order: number;
}

export interface VehicleSurcharge {
  vehicle_type: VehicleType;
  surcharge: number;
}
```

From jetwash24/supabase/seed.sql (service slugs and prices):
```
interior-express   base_price=15.00  duration=30
exterior-express   base_price=15.00  duration=45
exterior-premium   base_price=30.00  duration=90
exterior-interior  base_price=25.00  duration=75
interior-premium   base_price=75.00  duration=120
full-detailing     base_price=110.00 duration=150

surcharges: citadino=0, berlina=5, suv=10, carrinha=15
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install shadcn components + create catalog.ts pure functions + unit tests</name>
  <files>
    jetwash24/src/lib/catalog.ts
    jetwash24/src/__tests__/catalog.test.ts
    jetwash24/src/components/ui/tabs.tsx
    jetwash24/src/components/ui/card.tsx
    jetwash24/src/components/ui/badge.tsx
    jetwash24/src/components/ui/separator.tsx
  </files>
  <read_first>
    jetwash24/src/types/database.ts
    jetwash24/supabase/seed.sql
    jetwash24/vitest.config.ts
    jetwash24/package.json
  </read_first>
  <behavior>
    - calculatePrice({ base_price: 15 }, 0) returns 15
    - calculatePrice({ base_price: 15 }, 5) returns 20
    - calculatePrice({ base_price: 110 }, 15) returns 125
    - calculateSavings('exterior-interior', allServices, 0) returns 5 (15+15-25)
    - calculateSavings('exterior-interior', allServices, 5) returns 10 ((15+5)+(15+5)-(25+5))
    - calculateSavings('full-detailing', allServices, 0) returns 30 (15+30+75-110... wait: exterior-premium 30 + interior-premium 75 = 105 - 110 = -5... that's negative)
    - Actually: full-detailing components are ['exterior-premium', 'interior-premium']. (30+0)+(75+0)=105, bundle=110+0=110, saving = 105-110 = -5. That's negative (no saving for citadino). For berlina: (30+5)+(75+5)=115, bundle=110+5=115, saving=0. For SUV: (30+10)+(75+10)=125, bundle=110+10=120, saving=5.
    - calculateSavings returns 0 when saving is negative or zero (never show negative badge)
    - SLUG_CATEGORY['interior-express'] returns 'interior'
    - SLUG_CATEGORY['exterior-interior'] returns 'pacotes'
    - PACKAGE_COMPONENTS['exterior-interior'] returns ['exterior-express', 'interior-express']
    - PACKAGE_COMPONENTS['full-detailing'] returns ['exterior-premium', 'interior-premium']
  </behavior>
  <action>
    **Step 1: Install shadcn components.**
    Run: `cd /Users/augustinagapii/Desktop/ClaudeCode/jetwash24 && npx shadcn@latest add tabs card badge separator`
    This installs 4 UI components into `src/components/ui/`.

    **Step 2: Create `jetwash24/src/lib/catalog.ts`** with these exports:

    ```typescript
    import type { Service } from '@/types/database';

    export const SLUG_CATEGORY: Record<string, 'interior' | 'exterior' | 'pacotes'> = {
      'interior-express': 'interior',
      'interior-premium': 'interior',
      'exterior-express': 'exterior',
      'exterior-premium': 'exterior',
      'exterior-interior': 'pacotes',
      'full-detailing': 'pacotes',
    };

    export const PACKAGE_COMPONENTS: Record<string, string[]> = {
      'exterior-interior': ['exterior-express', 'interior-express'],
      'full-detailing': ['exterior-premium', 'interior-premium'],
    };

    export function calculatePrice(service: Pick<Service, 'base_price'>, surcharge: number): number {
      return service.base_price + surcharge;
    }

    export function calculateSavings(
      packageSlug: string,
      allServices: Pick<Service, 'slug' | 'base_price'>[],
      surcharge: number
    ): number {
      const componentSlugs = PACKAGE_COMPONENTS[packageSlug];
      if (!componentSlugs) return 0;
      const individualTotal = componentSlugs.reduce((sum, slug) => {
        const svc = allServices.find(s => s.slug === slug);
        return sum + (svc ? svc.base_price + surcharge : 0);
      }, 0);
      const bundlePrice = allServices.find(s => s.slug === packageSlug)?.base_price ?? 0;
      const saving = individualTotal - (bundlePrice + surcharge);
      return Math.max(0, Math.round(saving));
    }
    ```

    **Step 3: Create `jetwash24/src/__tests__/catalog.test.ts`** with TDD tests:

    Tests must import `calculatePrice`, `calculateSavings`, `SLUG_CATEGORY`, `PACKAGE_COMPONENTS` from `@/lib/catalog`.

    Create a mock `allServices` array matching seed.sql exactly:
    ```typescript
    const MOCK_SERVICES = [
      { slug: 'interior-express', base_price: 15 },
      { slug: 'exterior-express', base_price: 15 },
      { slug: 'exterior-premium', base_price: 30 },
      { slug: 'exterior-interior', base_price: 25 },
      { slug: 'interior-premium', base_price: 75 },
      { slug: 'full-detailing', base_price: 110 },
    ];
    ```

    Test cases:
    - `calculatePrice` with surcharge 0, 5, 10, 15
    - `calculateSavings('exterior-interior', MOCK_SERVICES, 0)` equals 5
    - `calculateSavings('exterior-interior', MOCK_SERVICES, 5)` equals 10
    - `calculateSavings('full-detailing', MOCK_SERVICES, 0)` equals 0 (negative clamped to 0: 105-110=-5)
    - `calculateSavings('full-detailing', MOCK_SERVICES, 10)` equals 5 (125-120=5)
    - `calculateSavings('nonexistent', MOCK_SERVICES, 0)` equals 0
    - `SLUG_CATEGORY` maps all 6 slugs correctly
    - `PACKAGE_COMPONENTS` has correct composition arrays

    Run tests with RED first (write tests, create empty stubs), then GREEN (implement).
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/Desktop/ClaudeCode/jetwash24 && npx vitest run src/__tests__/catalog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - jetwash24/src/lib/catalog.ts contains `export function calculatePrice`
    - jetwash24/src/lib/catalog.ts contains `export function calculateSavings`
    - jetwash24/src/lib/catalog.ts contains `export const SLUG_CATEGORY`
    - jetwash24/src/lib/catalog.ts contains `export const PACKAGE_COMPONENTS`
    - jetwash24/src/__tests__/catalog.test.ts contains `calculatePrice` and `calculateSavings`
    - jetwash24/src/components/ui/tabs.tsx exists
    - jetwash24/src/components/ui/card.tsx exists
    - jetwash24/src/components/ui/badge.tsx exists
    - jetwash24/src/components/ui/separator.tsx exists
    - `cd jetwash24 && npx vitest run src/__tests__/catalog.test.ts` exits 0
  </acceptance_criteria>
  <done>All 4 shadcn components installed, catalog.ts exports 4 items, all unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Add all Phase 3 translation keys + enable smooth scroll</name>
  <files>
    jetwash24/messages/pt.json
    jetwash24/messages/en.json
    jetwash24/src/app/globals.css
  </files>
  <read_first>
    jetwash24/messages/pt.json
    jetwash24/messages/en.json
    jetwash24/src/app/globals.css
    .planning/phases/03-landing-page-service-catalog/03-UI-SPEC.md
    .planning/phases/03-landing-page-service-catalog/03-RESEARCH.md
  </read_first>
  <action>
    **Step 1: Update `jetwash24/messages/pt.json`** — preserve existing keys, add new ones:

    ```json
    {
      "HomePage": {
        "title": "Lavagem de carros profissional no Algarve",
        "subtitle": "Marcacao online em menos de 2 minutos",
        "cta": "Marcar agora",
        "tagline": "Detailing profissional, precos honestos.",
        "about": "A Jetwash24 nasce da paixao pelo detalhe automovel. Sediados em Guia, Portugal, acreditamos que cada veiculo merece um tratamento profissional a precos justos. Venha conhecer-nos.",
        "catalogCta": "Conhecer os Servicos",
        "badge_interior": "Lavagem Interior",
        "badge_exterior": "Lavagem Exterior",
        "badge_pacotes": "Pacotes"
      },
      "Services": {
        "tab_interior": "Interior",
        "tab_exterior": "Exterior",
        "tab_pacotes": "Pacotes",
        "vehicle_citadino": "Citadino",
        "vehicle_berlina": "Berlina",
        "vehicle_suv": "SUV",
        "vehicle_carrinha": "Carrinha",
        "saving": "Poupa {amount}\u20ac",
        "duration": "{min} min",
        "emptyState": "Nenhum servico disponivel.",
        "errorState": "Nao foi possivel carregar os servicos. Tente novamente mais tarde.",
        "extras_deepClean": "Limpeza Profunda Estofos",
        "extras_petHair": "Remocao de Pelos de Animais",
        "extras_windows": "Limpeza de Vidros Interiores",
        "extras_ozone": "Ozonizacao",
        "extras_addMin": "+{min} min",
        "extras_addPrice": "+{price}\u20ac",
        "extras_total": "+{price}\u20ac / +{min} min"
      },
      "Contact": {
        "title": "Contacto",
        "phone": "+351 928 380 478",
        "email": "jetwash24detailing@gmail.com",
        "address": "Guia, Portugal",
        "instagram": "@jetwash24detailing",
        "mapLink": "Ver no Google Maps"
      },
      "Nav": {
        "services": "Servicos",
        "contact": "Contacto",
        "openMenu": "Abrir menu de navegacao"
      },
      "Navigation": {
        "home": "Inicio",
        "services": "Servicos",
        "booking": "Marcar",
        "contact": "Contacto"
      },
      "Common": {
        "loading": "A carregar...",
        "error": "Ocorreu um erro",
        "locale_switch": "EN"
      }
    }
    ```

    **Step 2: Update `jetwash24/messages/en.json`** — same structure, English values:

    ```json
    {
      "HomePage": {
        "title": "Professional car wash in the Algarve",
        "subtitle": "Book online in under 2 minutes",
        "cta": "Book now",
        "tagline": "Professional detailing, honest prices.",
        "about": "Jetwash24 was born from a passion for automotive detailing. Based in Guia, Portugal, we believe every vehicle deserves professional care at honest prices. Come find us.",
        "catalogCta": "See Services",
        "badge_interior": "Interior Wash",
        "badge_exterior": "Exterior Wash",
        "badge_pacotes": "Packages"
      },
      "Services": {
        "tab_interior": "Interior",
        "tab_exterior": "Exterior",
        "tab_pacotes": "Packages",
        "vehicle_citadino": "City Car",
        "vehicle_berlina": "Saloon",
        "vehicle_suv": "SUV",
        "vehicle_carrinha": "Estate / Van",
        "saving": "Save {amount}\u20ac",
        "duration": "{min} min",
        "emptyState": "No services available.",
        "errorState": "Could not load services. Please try again later.",
        "extras_deepClean": "Deep Upholstery Clean",
        "extras_petHair": "Pet Hair Removal",
        "extras_windows": "Interior Glass Cleaning",
        "extras_ozone": "Ozone Treatment",
        "extras_addMin": "+{min} min",
        "extras_addPrice": "+{price}\u20ac",
        "extras_total": "+{price}\u20ac / +{min} min"
      },
      "Contact": {
        "title": "Contact",
        "phone": "+351 928 380 478",
        "email": "jetwash24detailing@gmail.com",
        "address": "Guia, Portugal",
        "instagram": "@jetwash24detailing",
        "mapLink": "View on Google Maps"
      },
      "Nav": {
        "services": "Services",
        "contact": "Contact",
        "openMenu": "Open navigation menu"
      },
      "Navigation": {
        "home": "Home",
        "services": "Services",
        "booking": "Book",
        "contact": "Contact"
      },
      "Common": {
        "loading": "Loading...",
        "error": "An error occurred",
        "locale_switch": "PT"
      }
    }
    ```

    **Step 3: Update `jetwash24/src/app/globals.css`** — add `scroll-behavior: smooth` to the html rule inside `@layer base`:

    Change the existing html rule from:
    ```css
    html {
      @apply font-sans;
    }
    ```
    to:
    ```css
    html {
      @apply font-sans;
      scroll-behavior: smooth;
    }
    ```

    Do NOT modify any other CSS rules or brand tokens.
  </action>
  <verify>
    <automated>cd /Users/augustinagapii/Desktop/ClaudeCode/jetwash24 && node -e "const pt=require('./messages/pt.json'); const en=require('./messages/en.json'); const keys=['HomePage.tagline','HomePage.catalogCta','Services.tab_interior','Services.saving','Contact.phone','Nav.services']; keys.forEach(k=>{const [ns,key]=k.split('.'); if(!pt[ns]?.[key]) throw new Error('Missing PT: '+k); if(!en[ns]?.[key]) throw new Error('Missing EN: '+k)}); console.log('All translation keys present')"</automated>
  </verify>
  <acceptance_criteria>
    - jetwash24/messages/pt.json contains "tagline": "Detailing profissional
    - jetwash24/messages/pt.json contains "catalogCta": "Conhecer os Servicos"
    - jetwash24/messages/pt.json contains "tab_interior": "Interior"
    - jetwash24/messages/pt.json contains "saving": "Poupa {amount}
    - jetwash24/messages/pt.json contains "extras_deepClean"
    - jetwash24/messages/pt.json contains "Nav" section with "services" and "contact" and "openMenu"
    - jetwash24/messages/en.json contains "tagline": "Professional detailing, honest prices."
    - jetwash24/messages/en.json contains "catalogCta": "See Services"
    - jetwash24/messages/en.json contains "vehicle_carrinha": "Estate / Van"
    - jetwash24/src/app/globals.css contains "scroll-behavior: smooth"
    - Existing keys (HomePage.title, HomePage.subtitle, Navigation.*, Common.*) still present in both files
  </acceptance_criteria>
  <done>Both translation files contain all Phase 3 keys in correct namespaces, globals.css has smooth scroll, no existing keys removed</done>
</task>

</tasks>

<verification>
- `cd jetwash24 && npx vitest run src/__tests__/catalog.test.ts` passes
- `cd jetwash24 && npx tsc --noEmit 2>&1 | head -20` shows no errors related to catalog.ts
- Both pt.json and en.json parse as valid JSON with all required namespaces
- globals.css contains `scroll-behavior: smooth`
</verification>

<success_criteria>
- 4 shadcn components installed (tabs, card, badge, separator)
- catalog.ts exports calculatePrice, calculateSavings, SLUG_CATEGORY, PACKAGE_COMPONENTS
- All unit tests for price and savings calculations pass
- PT and EN translation files contain all Phase 3 keys
- Smooth scroll enabled in globals.css
</success_criteria>

<output>
After completion, create `.planning/phases/03-landing-page-service-catalog/03-01-SUMMARY.md`
</output>
