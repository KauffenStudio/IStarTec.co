---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - jetwash24/public/logo.svg
  - jetwash24/public/logo-white.svg
  - jetwash24/public/favicon.svg
  - jetwash24/src/app/globals.css
  - jetwash24/src/components/Logo.tsx
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02

must_haves:
  truths:
    - "A Jetwash24 SVG logo asset exists in public/"
    - "The logo uses brand colors: dark blue #0B1F3A and/or cyan #00C8E0"
    - "A white variant of the logo exists for use on dark backgrounds"
    - "A Logo React component exists that renders the SVG inline"
    - "The design system color tokens in globals.css are complete with all semantic tokens needed by shadcn/ui"
  artifacts:
    - path: "jetwash24/public/logo.svg"
      provides: "Primary Jetwash24 logo (color version)"
      min_lines: 5
    - path: "jetwash24/public/logo-white.svg"
      provides: "White variant for dark navy backgrounds"
      min_lines: 5
    - path: "jetwash24/public/favicon.svg"
      provides: "Favicon/icon-only version of logo"
      min_lines: 3
    - path: "jetwash24/src/components/Logo.tsx"
      provides: "Reusable Logo component"
      exports: ["Logo"]
    - path: "jetwash24/src/app/globals.css"
      provides: "Complete design system with brand + semantic color tokens"
      contains: "--color-brand-navy"
  key_links:
    - from: "jetwash24/src/components/Logo.tsx"
      to: "jetwash24/public/logo.svg"
      via: "SVG rendered inline or via next/image"
      pattern: "logo"
    - from: "jetwash24/src/app/globals.css"
      to: "shadcn/ui components"
      via: "CSS custom properties consumed by component classes"
      pattern: "--color-primary"
---

<objective>
Create the Jetwash24 logo assets and finalize the design system configuration with complete color tokens for shadcn/ui integration.

Purpose: The logo (DSGN-01) establishes brand identity used across all pages, and the design system (DSGN-02) provides the dark blue / white / cyan color scheme that every UI component will inherit. Both must exist before any visible UI is built in Phase 3.

Output: SVG logo assets (color, white, favicon), a reusable Logo component, and a finalized globals.css with complete shadcn/ui-compatible theme tokens.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- From Plan 01-01: globals.css already has base @theme tokens -->
From jetwash24/src/app/globals.css (created in Plan 01-01):
```css
@theme {
  --color-brand-navy: #0B1F3A;
  --color-brand-cyan: #00C8E0;
  --color-brand-white: #FFFFFF;
  --color-brand-navy-light: #142E54;
  --color-brand-cyan-dark: #009BB0;
  --color-background: #0B1F3A;
  --color-foreground: #FFFFFF;
  --color-primary: #00C8E0;
  --color-primary-foreground: #0B1F3A;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Jetwash24 SVG logo assets</name>
  <files>
    jetwash24/public/logo.svg,
    jetwash24/public/logo-white.svg,
    jetwash24/public/favicon.svg
  </files>
  <read_first>
    .planning/PROJECT.md,
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Create three SVG logo files for Jetwash24. The brand direction from PROJECT.md: "azul escuro + branco + detalhes ciano, profissional, dinamico." The business is a car wash in Guia, Portugal.

1. Create `jetwash24/public/logo.svg` — the primary full logo:
   - Combined wordmark: "JETWASH" in bold + "24" as a visual accent
   - Include a stylized water droplet or spray motif to convey car washing
   - Primary colors: dark blue (#0B1F3A) for text, cyan (#00C8E0) for accent/water elements
   - Viewbox: 200x60 (landscape, suitable for header placement)
   - Must be clean, professional SVG — no embedded raster images
   - Include the text "JETWASH24" as part of the logo (can be `<text>` with a web-safe bold font or `<path>` letterforms)

2. Create `jetwash24/public/logo-white.svg` — white variant:
   - Same design as logo.svg but with white (#FFFFFF) text and cyan (#00C8E0) accents
   - For use on dark navy backgrounds (which is the primary site background)
   - Same viewbox dimensions

3. Create `jetwash24/public/favicon.svg` — icon-only version:
   - Simplified icon from the logo (e.g., the water droplet element or "J24" lettermark)
   - Square viewbox: 32x32
   - Uses cyan (#00C8E0) as primary color for visibility at small sizes
   - Simple enough to render clearly at 16x16 pixels

Design principles:
- Professional and dynamic (not playful/cartoon)
- Clean lines, modern sans-serif feel
- Water/washing visual metaphor integrated naturally
- Must look good at both header size (~40px height) and favicon size (16-32px)
  </action>
  <verify>
    <automated>test -f jetwash24/public/logo.svg && test -f jetwash24/public/logo-white.svg && test -f jetwash24/public/favicon.svg && grep -l "svg" jetwash24/public/logo.svg jetwash24/public/logo-white.svg jetwash24/public/favicon.svg | wc -l</automated>
  </verify>
  <done>
    - jetwash24/public/logo.svg exists and contains "<svg" opening tag
    - jetwash24/public/logo.svg contains "JETWASH" or "Jetwash" or "jetwash" text
    - jetwash24/public/logo.svg contains "#0B1F3A" or "#0b1f3a" (dark blue)
    - jetwash24/public/logo.svg contains "#00C8E0" or "#00c8e0" (cyan)
    - jetwash24/public/logo-white.svg exists and contains "<svg" opening tag
    - jetwash24/public/logo-white.svg contains "#FFFFFF" or "#ffffff" or "white"
    - jetwash24/public/favicon.svg exists and contains "<svg" opening tag
    - jetwash24/public/favicon.svg contains a viewBox (square aspect for favicon)
    - None of the SVG files contain "<image" tags (no embedded rasters)
    - All three files are valid XML (no syntax errors)
    - Three SVG logo assets created: full color logo, white variant for dark backgrounds, and favicon icon — all using brand colors, no raster images
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Logo component and finalize design system tokens</name>
  <files>
    jetwash24/src/components/Logo.tsx,
    jetwash24/src/app/globals.css
  </files>
  <read_first>
    jetwash24/src/app/globals.css,
    jetwash24/public/logo.svg,
    jetwash24/public/logo-white.svg
  </read_first>
  <action>
1. Create `jetwash24/src/components/Logo.tsx`:
   ```typescript
   import Image from 'next/image';

   type LogoProps = {
     variant?: 'color' | 'white';
     width?: number;
     height?: number;
     className?: string;
   };

   export function Logo({
     variant = 'white',
     width = 160,
     height = 48,
     className = '',
   }: LogoProps) {
     const src = variant === 'white' ? '/logo-white.svg' : '/logo.svg';

     return (
       <Image
         src={src}
         alt="Jetwash24"
         width={width}
         height={height}
         className={className}
         priority
       />
     );
   }
   ```

2. Update `jetwash24/src/app/globals.css` to add complete shadcn/ui semantic tokens. READ the existing file first (created in Plan 01-01), then replace with the full version that adds muted, accent, card, popover, destructive, border, input, ring, and radius tokens:

   ```css
   @import "tailwindcss";

   @theme {
     /* ============================================
      * Jetwash24 Brand Palette (DSGN-02)
      * ============================================ */
     --color-brand-navy: #0B1F3A;
     --color-brand-cyan: #00C8E0;
     --color-brand-white: #FFFFFF;
     --color-brand-navy-light: #142E54;
     --color-brand-cyan-dark: #009BB0;
     --color-brand-gray: #94A3B8;

     /* ============================================
      * Shadcn/ui Semantic Tokens (dark theme)
      * Maps brand palette to component system
      * ============================================ */

     /* Page background and text */
     --color-background: #0B1F3A;
     --color-foreground: #FFFFFF;

     /* Primary action color (buttons, links) */
     --color-primary: #00C8E0;
     --color-primary-foreground: #0B1F3A;

     /* Secondary / muted surfaces */
     --color-secondary: #142E54;
     --color-secondary-foreground: #FFFFFF;
     --color-muted: #142E54;
     --color-muted-foreground: #94A3B8;

     /* Accent highlights */
     --color-accent: #142E54;
     --color-accent-foreground: #00C8E0;

     /* Cards and popovers */
     --color-card: #0F2847;
     --color-card-foreground: #FFFFFF;
     --color-popover: #0F2847;
     --color-popover-foreground: #FFFFFF;

     /* Destructive actions */
     --color-destructive: #EF4444;
     --color-destructive-foreground: #FFFFFF;

     /* Borders, inputs, focus rings */
     --color-border: #1E3A5F;
     --color-input: #1E3A5F;
     --color-ring: #00C8E0;

     /* Border radius */
     --radius: 0.5rem;
   }
   ```

   IMPORTANT: Do NOT create a tailwind.config.js or tailwind.config.ts file. Tailwind v4 is CSS-native.

3. Verify the build still passes with the updated globals.css:
   ```
   cd jetwash24 && npx tsc --noEmit
   ```
  </action>
  <verify>
    <automated>cd jetwash24 && npx tsc --noEmit 2>&1 | tail -3 && grep -c "\-\-color-brand-navy\|--color-primary\|--color-card\|--color-destructive\|--color-border\|--radius" src/app/globals.css</automated>
  </verify>
  <done>
    - jetwash24/src/components/Logo.tsx contains "export function Logo"
    - jetwash24/src/components/Logo.tsx contains "variant?: 'color' | 'white'"
    - jetwash24/src/components/Logo.tsx contains "/logo-white.svg"
    - jetwash24/src/components/Logo.tsx contains "/logo.svg"
    - jetwash24/src/components/Logo.tsx contains "alt=\"Jetwash24\""
    - jetwash24/src/app/globals.css contains "--color-brand-navy: #0B1F3A"
    - jetwash24/src/app/globals.css contains "--color-brand-cyan: #00C8E0"
    - jetwash24/src/app/globals.css contains "--color-primary: #00C8E0"
    - jetwash24/src/app/globals.css contains "--color-card:"
    - jetwash24/src/app/globals.css contains "--color-popover:"
    - jetwash24/src/app/globals.css contains "--color-destructive:"
    - jetwash24/src/app/globals.css contains "--color-border:"
    - jetwash24/src/app/globals.css contains "--color-input:"
    - jetwash24/src/app/globals.css contains "--color-ring:"
    - jetwash24/src/app/globals.css contains "--color-muted:"
    - jetwash24/src/app/globals.css contains "--color-secondary:"
    - jetwash24/src/app/globals.css contains "--radius:"
    - jetwash24/src/app/globals.css does NOT contain "tailwind.config"
    - No file jetwash24/tailwind.config.js exists
    - No file jetwash24/tailwind.config.ts exists
    - npx tsc --noEmit exits 0
    - Logo component exports with color/white variants, globals.css has complete shadcn/ui-compatible semantic tokens for the dark blue/white/cyan theme, TypeScript compiles clean
  </done>
</task>

</tasks>

<verification>
1. `test -f jetwash24/public/logo.svg && echo PASS`
2. `test -f jetwash24/public/logo-white.svg && echo PASS`
3. `test -f jetwash24/public/favicon.svg && echo PASS`
4. `grep "export function Logo" jetwash24/src/components/Logo.tsx`
5. `grep -c "\-\-color-" jetwash24/src/app/globals.css` returns 20+ (all semantic tokens)
6. `test ! -f jetwash24/tailwind.config.js && test ! -f jetwash24/tailwind.config.ts && echo PASS`
7. `cd jetwash24 && npm run build` exits 0
</verification>

<success_criteria>
- Three SVG logo assets exist (color, white, favicon) using brand colors
- Logo component is reusable with color/white variants via props
- globals.css contains all shadcn/ui semantic tokens mapped to brand palette
- No tailwind.config.js exists (Tailwind v4 CSS-native)
- Project builds successfully with the design system in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
