---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - jetwash24/package.json
  - jetwash24/next.config.ts
  - jetwash24/src/proxy.ts
  - jetwash24/src/i18n/routing.ts
  - jetwash24/src/i18n/navigation.ts
  - jetwash24/src/i18n/request.ts
  - jetwash24/src/app/layout.tsx
  - jetwash24/src/app/[locale]/layout.tsx
  - jetwash24/src/app/[locale]/page.tsx
  - jetwash24/messages/pt.json
  - jetwash24/messages/en.json
  - jetwash24/src/app/globals.css
  - jetwash24/src/lib/supabase.ts
  - jetwash24/.env.local
  - jetwash24/.env.example
  - jetwash24/vitest.config.ts
  - jetwash24/src/__tests__/i18n.test.ts
  - jetwash24/src/__tests__/schema.test.ts
  - jetwash24/src/__tests__/exclusion.test.ts
autonomous: true
requirements:
  - CONT-03
  - DSGN-02

must_haves:
  truths:
    - "Navigating to /pt serves Portuguese locale content via next-intl"
    - "Navigating to /en serves English locale content via next-intl"
    - "No hardcoded user-facing strings exist in .tsx files — all behind t() calls"
    - "npm run build succeeds without errors"
    - "npm run dev serves a placeholder home page at localhost:3000/pt"
  artifacts:
    - path: "jetwash24/src/proxy.ts"
      provides: "next-intl middleware for locale routing (Next.js 16 proxy.ts)"
      contains: "createMiddleware"
    - path: "jetwash24/src/i18n/routing.ts"
      provides: "Locale routing config"
      contains: "defineRouting"
    - path: "jetwash24/src/i18n/navigation.ts"
      provides: "Locale-aware navigation exports"
      exports: ["Link", "redirect", "usePathname", "useRouter"]
    - path: "jetwash24/src/i18n/request.ts"
      provides: "Server-side locale resolution"
      contains: "getRequestConfig"
    - path: "jetwash24/messages/pt.json"
      provides: "Portuguese translations"
      contains: "HomePage"
    - path: "jetwash24/messages/en.json"
      provides: "English translations"
      contains: "HomePage"
    - path: "jetwash24/src/app/[locale]/layout.tsx"
      provides: "Locale layout with NextIntlClientProvider"
      contains: "NextIntlClientProvider"
    - path: "jetwash24/src/lib/supabase.ts"
      provides: "SSR-safe Supabase server client"
      contains: "createServerClient"
    - path: "jetwash24/src/__tests__/i18n.test.ts"
      provides: "Wave 0 test stubs for locale routing"
      contains: "it.todo"
    - path: "jetwash24/src/__tests__/schema.test.ts"
      provides: "Wave 0 test stubs for schema validation"
      contains: "it.todo"
    - path: "jetwash24/src/__tests__/exclusion.test.ts"
      provides: "Wave 0 test stubs for exclusion constraint"
      contains: "it.todo"
  key_links:
    - from: "jetwash24/src/proxy.ts"
      to: "jetwash24/src/i18n/routing.ts"
      via: "import routing config"
      pattern: "from.*./i18n/routing"
    - from: "jetwash24/src/app/[locale]/layout.tsx"
      to: "jetwash24/src/i18n/routing.ts"
      via: "import for locale validation"
      pattern: "from.*@/i18n/routing"
    - from: "jetwash24/next.config.ts"
      to: "jetwash24/src/i18n/request.ts"
      via: "next-intl plugin wiring"
      pattern: "createNextIntlPlugin"
---

<objective>
Scaffold a new Next.js 16 project with TypeScript, Tailwind v4, shadcn/ui, next-intl i18n routing, Supabase client, and Vitest — producing a working bilingual placeholder page.

Purpose: Every downstream phase depends on i18n being wired before the first string, Tailwind v4 CSS theming being in place, and the Supabase client existing. Retrofitting any of these is a near-rewrite.

Output: A runnable Next.js 16 app at `jetwash24/` with `/pt` and `/en` routes serving locale-specific content, shadcn/ui initialized, Supabase server client configured, and Vitest ready for tests with Wave 0 stubs.
</objective>

<execution_context>
@/Users/augustinagapii/.claude/get-shit-done/workflows/execute-plan.md
@/Users/augustinagapii/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project and install all dependencies</name>
  <files>
    jetwash24/package.json,
    jetwash24/.env.local,
    jetwash24/.env.example
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
1. Archive the existing jetwash24 folder if it exists:
   ```
   mv jetwash24 jetwash24-legacy 2>/dev/null || true
   ```

2. Scaffold a new Next.js project:
   ```
   npx create-next-app@latest jetwash24 --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"
   ```
   This creates Next.js 16.2.0 with React 19, Tailwind v4, and TypeScript.

3. Install runtime dependencies:
   ```
   cd jetwash24
   npm install @supabase/supabase-js @supabase/ssr next-intl date-fns
   ```

4. Install dev dependencies for testing:
   ```
   npm install -D vitest @vitest/coverage-v8
   ```

5. Initialize shadcn/ui:
   ```
   npx shadcn@latest init -t next
   ```
   Accept defaults. This configures `components.json` and adds the `src/components/ui/` directory. It must NOT create a `tailwind.config.js` file (Tailwind v4 is CSS-native).

6. Create `jetwash24/.env.local` with placeholder Supabase credentials:
   ```
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   ```

7. Create `jetwash24/.env.example` with the same keys but empty values:
   ```
   NEXT_PUBLIC_SUPABASE_URL=
   NEXT_PUBLIC_SUPABASE_ANON_KEY=
   ```

8. Create `jetwash24/vitest.config.ts`:
   ```typescript
   import { defineConfig } from 'vitest/config';
   import path from 'path';

   export default defineConfig({
     test: {
       environment: 'node',
       include: ['src/**/*.test.ts', 'src/**/*.test.tsx'],
     },
     resolve: {
       alias: {
         '@': path.resolve(__dirname, './src'),
       },
     },
   });
   ```

9. Add test script to `package.json` scripts:
   ```json
   "test": "vitest run",
   "test:coverage": "vitest run --coverage"
   ```

10. Verify: No `tailwind.config.js` or `tailwind.config.ts` file exists in the project root. If shadcn created one, delete it — Tailwind v4 is CSS-native. If `middleware.ts` was scaffolded, delete it (will be replaced by `proxy.ts` in Task 2).

11. Verify `npx tsc --noEmit` succeeds (it should with the default scaffolded pages).
  </action>
  <verify>
    <automated>cd jetwash24 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    - jetwash24/package.json contains "next-intl" in dependencies
    - jetwash24/package.json contains "@supabase/supabase-js" in dependencies
    - jetwash24/package.json contains "@supabase/ssr" in dependencies
    - jetwash24/package.json contains "date-fns" in dependencies
    - jetwash24/package.json contains "vitest" in devDependencies
    - jetwash24/.env.example contains "NEXT_PUBLIC_SUPABASE_URL="
    - jetwash24/.env.example contains "NEXT_PUBLIC_SUPABASE_ANON_KEY="
    - jetwash24/vitest.config.ts contains "defineConfig"
    - jetwash24/components.json exists (shadcn/ui initialized)
    - No file named tailwind.config.js or tailwind.config.ts exists in jetwash24/
    - No file named middleware.ts exists in jetwash24/src/
    - npx tsc --noEmit exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 vitest stub files for i18n, schema, and exclusion tests</name>
  <files>
    jetwash24/src/__tests__/i18n.test.ts,
    jetwash24/src/__tests__/schema.test.ts,
    jetwash24/src/__tests__/exclusion.test.ts
  </files>
  <action>
Create the `jetwash24/src/__tests__/` directory and three Wave 0 test stub files referenced in 01-VALIDATION.md. These contain `it.todo()` entries that downstream plans will flesh out.

1. Create `jetwash24/src/__tests__/i18n.test.ts`:
   ```typescript
   import { describe, it } from 'vitest';

   describe('i18n locale routing', () => {
     it.todo('routing config defines pt and en locales');
     it.todo('routing config sets pt as default locale');
     it.todo('messages/pt.json contains all required translation keys');
     it.todo('messages/en.json contains all required translation keys');
     it.todo('pt.json and en.json have identical key structures');
   });
   ```

2. Create `jetwash24/src/__tests__/schema.test.ts`:
   ```typescript
   import { describe, it } from 'vitest';

   describe('database schema validation', () => {
     it.todo('services table has required columns: id, slug, name_pt, name_en, duration_min, base_price');
     it.todo('vehicle_surcharges table has required columns: vehicle_type, surcharge');
     it.todo('bookings table has required columns: id, service_id, vehicle_type, start_time, end_time, status');
     it.todo('bookings status constraint allows only confirmed and cancelled');
     it.todo('seed data contains 6 services matching PROJECT.md catalog');
     it.todo('seed data contains 4 vehicle surcharge rows');
   });
   ```

3. Create `jetwash24/src/__tests__/exclusion.test.ts`:
   ```typescript
   import { describe, it } from 'vitest';

   describe('TECH-01: booking exclusion constraint', () => {
     it.todo('migration enables btree_gist extension before creating tables');
     it.todo('exclusion constraint uses tstzrange on start_time and end_time');
     it.todo('exclusion constraint only applies to confirmed bookings');
     it.todo('overlapping confirmed bookings are rejected by the constraint');
     it.todo('cancelled bookings do not trigger the exclusion constraint');
   });
   ```
  </action>
  <verify>
    <automated>cd jetwash24 && npx vitest run src/__tests__/ 2>&1 | tail -10</automated>
  </verify>
  <done>
    - jetwash24/src/__tests__/i18n.test.ts exists with it.todo() entries for locale routing
    - jetwash24/src/__tests__/schema.test.ts exists with it.todo() entries for schema validation
    - jetwash24/src/__tests__/exclusion.test.ts exists with it.todo() entries for TECH-01 exclusion constraint
    - npx vitest run src/__tests__/ exits 0 (all todos are skipped, none fail)
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire next-intl i18n routing, Supabase client, and placeholder home page</name>
  <files>
    jetwash24/next.config.ts,
    jetwash24/src/proxy.ts,
    jetwash24/src/i18n/routing.ts,
    jetwash24/src/i18n/navigation.ts,
    jetwash24/src/i18n/request.ts,
    jetwash24/src/app/layout.tsx,
    jetwash24/src/app/[locale]/layout.tsx,
    jetwash24/src/app/[locale]/page.tsx,
    jetwash24/messages/pt.json,
    jetwash24/messages/en.json,
    jetwash24/src/lib/supabase.ts,
    jetwash24/src/app/globals.css
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md,
    jetwash24/next.config.ts,
    jetwash24/src/app/globals.css
  </read_first>
  <action>
1. Create `jetwash24/src/i18n/routing.ts`:
   ```typescript
   import {defineRouting} from 'next-intl/routing';

   export const routing = defineRouting({
     locales: ['pt', 'en'],
     defaultLocale: 'pt'
   });
   ```

2. Create `jetwash24/src/proxy.ts` (NOT middleware.ts — Next.js 16):
   ```typescript
   import createMiddleware from 'next-intl/middleware';
   import {routing} from './i18n/routing';

   export default createMiddleware(routing);

   export const config = {
     matcher: '/((?!api|_next|_vercel|.*\\..*).*)'
   };
   ```

3. Create `jetwash24/src/i18n/navigation.ts`:
   ```typescript
   import {createNavigation} from 'next-intl/navigation';
   import {routing} from './routing';

   export const {Link, redirect, usePathname, useRouter, getPathname} =
     createNavigation(routing);
   ```

4. Create `jetwash24/src/i18n/request.ts`:
   ```typescript
   import {getRequestConfig} from 'next-intl/server';
   import {hasLocale} from 'next-intl';
   import {routing} from './routing';

   export default getRequestConfig(async ({requestLocale}) => {
     const requested = await requestLocale;
     const locale = hasLocale(routing.locales, requested)
       ? requested
       : routing.defaultLocale;
     return {locale};
   });
   ```

5. Update `jetwash24/next.config.ts` to wire next-intl plugin:
   ```typescript
   import {NextConfig} from 'next';
   import createNextIntlPlugin from 'next-intl/plugin';

   const nextConfig: NextConfig = {};
   const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');
   export default withNextIntl(nextConfig);
   ```

6. Create `jetwash24/messages/pt.json`:
   ```json
   {
     "HomePage": {
       "title": "Lavagem de carros profissional no Algarve",
       "subtitle": "Marcacao online em menos de 2 minutos",
       "cta": "Marcar agora"
     },
     "Navigation": {
       "home": "Inicio",
       "services": "Servicos",
       "booking": "Marcar",
       "contact": "Contacto"
     },
     "Common": {
       "loading": "A carregar...",
       "error": "Ocorreu um erro",
       "locale_switch": "EN"
     }
   }
   ```

7. Create `jetwash24/messages/en.json`:
   ```json
   {
     "HomePage": {
       "title": "Professional car wash in the Algarve",
       "subtitle": "Book online in under 2 minutes",
       "cta": "Book now"
     },
     "Navigation": {
       "home": "Home",
       "services": "Services",
       "booking": "Book",
       "contact": "Contact"
     },
     "Common": {
       "loading": "Loading...",
       "error": "An error occurred",
       "locale_switch": "PT"
     }
   }
   ```

8. Replace `jetwash24/src/app/layout.tsx` with a minimal root layout:
   ```typescript
   import type {Metadata} from 'next';

   export const metadata: Metadata = {
     title: 'Jetwash24',
     description: 'Professional car wash booking — Guia, Portugal',
   };

   export default function RootLayout({children}: {children: React.ReactNode}) {
     return children;
   }
   ```
   IMPORTANT: The root layout must NOT have `<html>` or `<body>` tags — those are in the locale layout.

9. Create `jetwash24/src/app/[locale]/layout.tsx`:
   ```typescript
   import {NextIntlClientProvider, hasLocale} from 'next-intl';
   import {notFound} from 'next/navigation';
   import {routing} from '@/i18n/routing';
   import {setRequestLocale} from 'next-intl/server';
   import '@/app/globals.css';

   type Props = {
     children: React.ReactNode;
     params: Promise<{locale: string}>;
   };

   export function generateStaticParams() {
     return routing.locales.map((locale) => ({locale}));
   }

   export default async function LocaleLayout({children, params}: Props) {
     const {locale} = await params;
     if (!hasLocale(routing.locales, locale)) {
       notFound();
     }
     setRequestLocale(locale);

     return (
       <html lang={locale}>
         <body className="bg-brand-navy text-brand-white min-h-screen antialiased">
           <NextIntlClientProvider>{children}</NextIntlClientProvider>
         </body>
       </html>
     );
   }
   ```

10. Create `jetwash24/src/app/[locale]/page.tsx`:
    ```typescript
    import {useTranslations} from 'next-intl';
    import {setRequestLocale} from 'next-intl/server';

    type Props = {params: Promise<{locale: string}>};

    export default async function HomePage({params}: Props) {
      const {locale} = await params;
      setRequestLocale(locale);
      const t = useTranslations('HomePage');

      return (
        <main className="flex min-h-screen flex-col items-center justify-center p-8">
          <h1 className="text-4xl font-bold text-brand-cyan mb-4">{t('title')}</h1>
          <p className="text-xl text-brand-white/80 mb-8">{t('subtitle')}</p>
          <button className="bg-brand-cyan text-brand-navy font-semibold px-8 py-3 rounded-lg hover:bg-brand-cyan-dark transition-colors">
            {t('cta')}
          </button>
        </main>
      );
    }
    ```

11. Update `jetwash24/src/app/globals.css` — replace the entire file content with:
    ```css
    @import "tailwindcss";

    @theme {
      /* Jetwash24 brand palette (DSGN-02) */
      --color-brand-navy: #0B1F3A;
      --color-brand-cyan: #00C8E0;
      --color-brand-white: #FFFFFF;
      --color-brand-navy-light: #142E54;
      --color-brand-cyan-dark: #009BB0;

      /* Shadcn/ui semantic tokens */
      --color-background: #0B1F3A;
      --color-foreground: #FFFFFF;
      --color-primary: #00C8E0;
      --color-primary-foreground: #0B1F3A;
    }
    ```
    Do NOT add a `tailwind.config.js` — Tailwind v4 is CSS-native.

12. Create `jetwash24/src/lib/supabase.ts`:
    ```typescript
    import {createServerClient} from '@supabase/ssr';
    import {cookies} from 'next/headers';
    import {createClient} from '@supabase/supabase-js';

    // Server-side client (use in Server Components, Route Handlers, Server Actions)
    export async function createSupabaseServerClient() {
      const cookieStore = await cookies();
      return createServerClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
        {
          cookies: {
            getAll() {
              return cookieStore.getAll();
            },
            setAll(cookiesToSet) {
              cookiesToSet.forEach(({name, value, options}) =>
                cookieStore.set(name, value, options)
              );
            },
          },
        }
      );
    }

    // Browser-side client (use in Client Components)
    export function createSupabaseBrowserClient() {
      return createClient(
        process.env.NEXT_PUBLIC_SUPABASE_URL!,
        process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
      );
    }
    ```

13. Delete any scaffolded `jetwash24/src/app/page.tsx` (top-level page outside `[locale]`).

14. Run `npm run build` to verify the entire i18n wiring compiles and builds.
  </action>
  <verify>
    <automated>cd jetwash24 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    - jetwash24/src/proxy.ts contains "createMiddleware"
    - jetwash24/src/proxy.ts contains "from './i18n/routing'"
    - jetwash24/src/i18n/routing.ts contains "defineRouting"
    - jetwash24/src/i18n/routing.ts contains "'pt', 'en'"
    - jetwash24/src/i18n/routing.ts contains "defaultLocale: 'pt'"
    - jetwash24/src/i18n/navigation.ts contains "createNavigation"
    - jetwash24/src/i18n/request.ts contains "getRequestConfig"
    - jetwash24/next.config.ts contains "createNextIntlPlugin"
    - jetwash24/next.config.ts contains "./src/i18n/request.ts"
    - jetwash24/src/app/[locale]/layout.tsx contains "NextIntlClientProvider"
    - jetwash24/src/app/[locale]/layout.tsx contains "generateStaticParams"
    - jetwash24/src/app/[locale]/layout.tsx contains "setRequestLocale"
    - jetwash24/src/app/[locale]/page.tsx contains "useTranslations('HomePage')"
    - jetwash24/src/app/[locale]/page.tsx does NOT contain any literal Portuguese or English strings (all behind t() calls)
    - jetwash24/messages/pt.json contains "Lavagem de carros profissional"
    - jetwash24/messages/en.json contains "Professional car wash"
    - jetwash24/src/app/globals.css contains "--color-brand-navy: #0B1F3A"
    - jetwash24/src/app/globals.css contains "--color-brand-cyan: #00C8E0"
    - jetwash24/src/lib/supabase.ts contains "createServerClient"
    - jetwash24/src/lib/supabase.ts contains "createSupabaseBrowserClient"
    - jetwash24/.env.example contains "NEXT_PUBLIC_SUPABASE_URL"
    - No file jetwash24/src/middleware.ts exists
    - No file jetwash24/tailwind.config.js exists
    - No file jetwash24/src/app/page.tsx exists (only [locale]/page.tsx)
    - npm run build exits 0
  </done>
</task>

</tasks>

<verification>
1. `cd jetwash24 && npx tsc --noEmit` exits 0
2. `cd jetwash24 && npx vitest run src/__tests__/ 2>&1 | tail -5` exits 0 (todo stubs pass)
3. `cd jetwash24 && npm run build` exits 0
4. `grep -r "createMiddleware" jetwash24/src/proxy.ts` returns a match
5. `grep -r "defineRouting" jetwash24/src/i18n/routing.ts` returns a match
6. `grep -rn "Lavagem\|Professional car\|Marcar agora\|Book now" jetwash24/src/app/` returns 0 results (no hardcoded strings in components)
7. `test -f jetwash24/messages/pt.json && test -f jetwash24/messages/en.json && echo PASS`
8. `test ! -f jetwash24/tailwind.config.js && echo PASS`
9. `test ! -f jetwash24/src/middleware.ts && echo PASS`
10. `test -f jetwash24/src/__tests__/i18n.test.ts && test -f jetwash24/src/__tests__/schema.test.ts && test -f jetwash24/src/__tests__/exclusion.test.ts && echo PASS`
</verification>

<success_criteria>
- The project builds and runs with `npm run dev`
- Visiting /pt shows Portuguese placeholder content
- Visiting /en shows English placeholder content
- All user-facing strings are in messages/*.json, not in .tsx files
- Tailwind v4 brand colors are defined in globals.css via @theme
- Supabase server + browser clients are ready for Phase 1 Plan 02
- Vitest is configured and ready for test files
- Wave 0 test stubs exist for i18n, schema, and exclusion constraint (TECH-01)
- `npx vitest run` passes with all stubs in todo state
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
